# GPT 施工日志

本文件只由 GPT 追加。历史条目不重写；需要纠正时新增更正条目。

## 2026-07-28｜主页 CG 按时段关闭

- 执行者：GPT
- 分支：`homepage-rebuild`
- 提交：见该分支当日历史
- 涉及文件：`index.html`
- 用户目标：CG banner 在同一时间段关闭后，进入其他系统再返回主页时不要重复出现；下一个时间段再显示下一张。
- 实际改动：使用日期与 morning/day/evening/night 组合生成时段键；22:00–次日 06:00 视为同一个夜间时段；关闭状态写入本地存储。
- 验证：检查同一时段重载、跨页面返回和跨时段逻辑。
- 遗留：无。
- 请勿误改：不要恢复成“每次页面加载都显示”。

## 2026-07-28｜角色房间通用分支与响应式 Will 房间

- 执行者：GPT
- 分支：`homepage-rebuild`
- 提交：`465666263cc1da43aa7b365aff7ba47b14ea3a35`
- 涉及文件：`index.html`
- 用户目标：Will 的房间在桌面位于右侧、手机位于下方，并形成以后增加角色房间可复用的规则。
- 实际改动：建立 top、left、adaptive 三个分支容器；同分支房间可以重叠；连接按钮跟随 Natumi 房间边缘响应式换位。
- 验证：桌面与手机浏览器截图、房间边缘和连接按钮位置检查。
- 遗留：同一方向多房间的选择器尚未制作。
- 请勿误改：手机的 Will 房间仍应在下方。

## 2026-07-28｜桌面房间固定骨架

- 执行者：GPT
- 分支：`homepage-rebuild`
- 提交：`2162b53eb766eff6a3ac91aeda5c408c63c710f7`
- 涉及文件：`index.html`
- 用户目标：桌面未连接 Will 时仍使用大版图和右侧连接按钮；关闭 Neal、Jane 或 Will 不改变其他房间尺寸。
- 实际改动：桌面版图改为不依赖房间开关的固定网格；关闭房间只留下空位；删除会触发布局收缩的状态绑定。
- 验证：全开、分别关闭 Will／Neal／Jane 及手机竖屏；各桌面状态版图、banner 和未关闭房间尺寸一致。
- 遗留：无。
- 请勿误改：空房间区域允许形成不规则留白，不要让剩余房间自动填满。

## 2026-07-28｜Kitchen 审查与三项收尾修复

- 执行者：GPT
- 分支：尚未提交到 GitHub
- 提交：无
- 涉及文件：用户共享的 `kitchen(4).html`
- 用户目标：复核 Claude Cowork 对 kitchen 的修改，并补完普通备注、删除后的页面状态和图片云端路径。
- 实际改动：
  - 普通 `item.note` 在详情页作为“品目备注”独立显示。
  - 删除当前项目后清理编辑状态并返回项目列表。
  - 品目图片使用稳定 `item.id` 作为云端路径；旧品目缺 ID 时自动生成并固化。
- 验证：
  - 内联脚本语法通过。
  - 无原生 `confirm()`／`prompt()` 调用。
  - 跨分栏双向拖拽及同栏换序计数通过。
  - 打卡照片选择、保存和详情显示通过。
  - 新旧品目图片路径与项目删除导航通过。
- 遗留：修正版目前是用户共享文件，尚未替换 GitHub `main` 中的 `kitchen.html`。
- 请勿误改：`item.note` 与 `checkinNote` 是两种不同备注。

## 2026-07-28｜Kitchen 修正版同步完成

- 执行者：GPT
- 分支：`main`、`homepage-rebuild`
- 提交：`main dbde830c6d54f2ac55410255fe6ca9fbd835a4ad`；`homepage-rebuild d4803b8adb9d71dba85010555f2c486b8e2193ce`
- 涉及文件：`kitchen.html`
- 用户目标：保留 Claude Cowork 已完成的 Kitchen 主体改造，并将 GPT 三项收尾修复同步到正式页与主页施工分支。
- 实际改动：两个分支写入同一份最终文件；Claude 的项目／品目拖拽、空格隐藏数字、减弱抖动、追加分栏 modal、分栏排序、自定义确认框、行内编辑和打卡照片均保留；叠加普通品目备注显示、删除当前项目后的导航清理和稳定图片 ID。
- 验证：两个分支的 `kitchen.html` blob SHA 均为 `0f0b5f1146646dc2dee53315c37255199b79e949`，与本地已测试成品一致。
- 遗留：无。
- 请勿误改：不要用压缩上下文后生成的 `kitchen(5).html` 覆盖此版本；`item.note` 与 `checkinNote` 仍是两种不同备注。

## 2026-07-29｜活动收集与记账系统第一版接入

- 执行者：GPT
- 分支：`main`
- 提交：见 `main` 当日历史
- 涉及文件：`event.html`、`wallet.html`、`index.html`、`cloud-sync.js`、`backup.html`、`docs/DECISIONS.md`
- 用户目标：将 Claude Cowork 的活动／记账毛坯接入正式程序，通过实际使用判断后续装修方向；旧活动逻辑已经玩完，可以由新版覆盖。
- 实际改动：
  - 新活动收集系统接管 `event.html`；旧 `event_db`、`event_presets` 仅保留为历史存档。
  - 活动分栏改用稳定 ID 重建，删除中间分栏不再让后续品目串栏。
  - 周边入手只让图片变彩，全部标记“已处理”后才计入完成并显示完成边框。
  - 活动品目与打卡记录加入图片选择、压缩、云端上传和详情展示；未完成品目也可进入详情编辑。
  - 新增 `wallet.html` 及主页入口；修复日本本地日期、旧存档结构补全、超额划拨和同一时段多人外出的余额竞争。
  - `activity_db`、`wallet_db` 纳入云同步与全量备份。
- 验证：五个改动文件脚本语法通过；分栏删除、周边边框、图片渲染、本地日期、钱包旧数据补全及多人外出余额测试通过。
- 遗留：活动和记账仍是第一版视觉；历史账目浏览、活动主题图和更完整的编辑体验等待实际使用后决定。
- 请勿误改：不要把 `event.html` 恢复为旧抽卡日课活动页；新版活动使用 `activity_db`，旧 `event_db` 只用于保留历史。

## 2026-08-22｜世界切换底层与 CBI 案卷空壳

- 执行者：GPT
- 分支：`feature/cbi-world-shell`
- 提交：`b0b4a4d55a3c6ec828d3eece11a4ae04c2111849`
- 涉及文件：`index.html`、`world-context.js`、`world-empty.html`、`cbi.html`、`cbi-data.js`、`cloud-sync.js`、`backup.html`、`tests/world-context.test.js`、`tests/world-shell-structure.test.js`、架构文档。
- 用户目标：不复制第二套主页，在夹缝空间与 CBI 办公室之间整体切换；保留同一份现实数据，同时为 CBI 剧情建立可增删改的案卷空壳。
- 实际改动：
  - 版头成为世界选择入口；世界切换只保存 `activeWorldId` 并重新渲染。
  - 同一个 `index.html` 新增 CBI 办公室场景，只复用一份 Jane 立绘；CBI 横幅留空，右侧栏目结构不变。
  - 活动、收藏仍走原有全局入口；CBI 尚未建立的日常、记账、饮食与奖励模块进入明确空状态，不读取夹缝空间模块数据。
  - 新增 `cbi_db`：当前案宗、案件档案、人物档案与自动汇总的主线记录；没有预填剧情或人物假数据。
  - 当前世界与 CBI 案卷纳入云同步和完整备份；可变入口排序改用稳定 ID，避免换路由后丢失用户排序。
- 验证：世界上下文、旧数据归属、CBI 主线派生、单主页结构、稳定入口 ID、云同步与备份共 9 项测试通过；三个 HTML 的内联脚本语法与重复 DOM ID 检查通过；`git diff --check` 通过。
- 遗留：当前运行环境没有可用 Chromium，未做真实浏览器截图；CBI 办公室与案卷页保留为轻量第一版，后续可只调整 CSS 与场景美术。
- 请勿误改：世界按钮不得迁移、复制、重命名或清空业务数据；未标记旧数据默认属于 `liminal`；不要让 CBI 空模块读取夹缝空间数据库；MAINLINE 必须继续从案宗 `mainlineStatus` 自动生成；不要复制第二份 Jane DOM。

## 2026-08-22｜CBI 世界内地点切换

- 执行者：GPT
- 分支：`main`
- 提交：`fae0ad59d62b7d157ff0def913f5196b3bd56741`
- 涉及文件：`index.html`、`world-context.js`、`backup.html`、`cbi.html`、`world-empty.html`、世界层测试与架构文档。
- 用户目标：将原本夹缝空间中的 `CBI × Boss` Jane 房间包迁入 CBI 世界，在 CBI 办公室与 Boss 家之间切换，并解决办公室仍播放居家台词的问题。
- 实际改动：CBI 注册 `office` 与 `home` 两个地点并分别记忆；旧房间包入口从夹缝空间退役，原客房／Boss房间样式与居家台词转为 Boss 家；办公室改用独立台词池；地点切换只改变场景、Jane位置和台词，不改变 CBI 路由或 `cbi_db`。
- 验证：世界／地点记忆、v1 世界存档兼容、数据边界、单 Jane DOM、地点入口与同步备份共 10 项测试通过；HTML 内联脚本语法、重复 DOM ID 与 `git diff --check` 通过。
- 遗留：办公室与 Boss 家继续沿用结构毛坯，等待只涉及 CSS 和场景装饰的视觉调整。
- 请勿误改：不要把地点重新拆成世界或独立数据库；办公室台词使用 `JANE_CBI_OFFICE_LINES`，Boss 家使用 `JANE_CBI_BOSS_LINES`；夹缝空间不要恢复旧 CBI 房间包入口。

## 2026-08-22｜CBI 地点标签字号修复

- 执行者：GPT
- 分支：`main`
- 提交：`130f31542424bff3ba424512bd2f20c6370e38a2`
- 涉及文件：`index.html`
- 实际改动：将地点按钮无效的 `font` 简写拆为有效的字体继承、字号和行高声明；文字恢复为小标签，点击热区尺寸保持不变。
- 验证：内联脚本语法与 `git diff --check` 通过。

## 2026-08-22｜Notes 展开时桌面布局防位移

- 执行者：GPT
- 分支：`main`
- 提交：`6f39cd05bacdec6556ee5c4a630c8b31aeb7cdfc`
- 涉及文件：`index.html`、`tests/world-shell-structure.test.js`
- 实际改动：桌面端预留稳定滚动条槽，避免 Notes 展开导致页面滚动条出现后居中网格整体左移；同时将 Notes 栏固定为 260px，并允许输入框和长文本在栏内收缩换行。
- 验证：11 项世界／首页结构测试、内联脚本语法与 `git diff --check` 通过。
- 请勿误改：不要通过移动左侧主页或动态改房间宽度抵消此问题；根因是滚动条出现与右栏最小内容宽度。

## 2026-08-22｜CBI 办公室与 Boss 家视觉装修

- 执行者：Claude 视觉草稿；GPT 结构审查与合并
- 分支：`main`
- 涉及文件：`index.html`
- 用户目标：在不改世界／地点切换与数据结构的前提下，将 CBI 办公室和 Boss 家从结构毛坯补成更完整的俯瞰场景。
- 实际改动：办公室补齐 Lisbon 工位、五把办公椅、卷宗柜、打印机、茶水区、植物、散落文件等装饰，并调整 Boss 大桌、团队工位与 Jane 沙发位置；Boss 家新增只在 CBI 世界显示的生活杂物与客房物件。没有修改 JavaScript、路由、存储键、台词池或 Jane DOM。
- 验证：交付版与合并后的 `index.html` SHA-256 完全一致；12 项世界／云同步／首页结构测试及 `git diff --check` 通过。
- 请勿误改：`.cbi-home-only` 装饰不得在夹缝空间显示；办公室继续复用唯一的 `#char-jane`，不要为场景装饰复制角色节点。

## 2026-08-22｜世界化饮食陪伴、沙拉项目与角色日程接入

- 执行者：GPT
- 分支：`main`
- 涉及文件：`meals.html`、`recipe.html`、`schedule.html`、`character-runtime.js`、`index.html`、`world-context.js`、`cloud-sync.js`、`backup.html`、世界层测试与架构文档。
- 用户目标：接入找回的沙拉盲盒、角色行程与日常饮食陪伴，同时保证切换世界不会丢失现实记录，也不会让两个世界的角色内容串在一起。
- 实际改动：
  - 原 `kitchen.html` 保留为全局餐厅／联名／菜品收藏，并在收藏页增加 Food 入口；主页日常“饮食”改进 `meals.html`。
  - `meal_log_db` 记录我的真实餐食并跨世界共用；`meal_companion_db` 按世界保存角色餐食、邀请和约饭。
  - 夹缝空间接入找回的 Neal／Jane／Will 饮食池与 18 张沙拉盲盒；CBI 使用独立空项目，不复制夹缝空间内容。
  - 日程存储升级为世界分层：个人现实事件全局可见，带角色的安排与角色固定行程按世界可见；旧数据迁入夹缝空间。
  - 只有不可移动的夹缝空间行程影响首页角色状态；普通约会和可移动事件不会让角色整日消失。
  - 新数据键全部纳入云同步和完整备份，没有预填 CBI 菜谱、饮食陪伴或行程。
- 验证：世界迁移、全局现实事件、世界角色事件、世界人物名单与固定行程锁定均有功能测试；HTML 内联脚本、JavaScript 语法、完整测试集与 `git diff --check` 通过。
- 请勿误改：不要把 `meal_log_db` 按世界拆分；不要把带角色的事件改为全局；不要在 CBI 自动复制夹缝空间沙拉或示例日程；`kitchen_db` 仍是全局收藏而非每日饮食陪伴。

## 2026-08-23｜饮食原稿视觉恢复与 CBI 房间风格统一

- 执行者：GPT
- 分支：`main`
- 涉及文件：`meals.html`、`index.html`、`tests/meals-world-separation.test.js`、`tests/world-shell-structure.test.js`。
- 用户目标：每日饮食陪伴继续使用 Claude 找回原稿的成熟美工；CBI办公室和 Boss 家必须跟随夹缝空间房间的字体与淡彩色块风格，不能另造视觉语言。
- 实际改动：
  - 饮食页恢复原稿的七日横排日历、内嵌日期详情、角色餐食标签、邀请卡、卡内日期选择、折叠分隔标题和菜谱项目行；移除后来另搭的大卡片外壳与原生 `prompt()`。
  - 保留 `meal_log_db` 全局事实层和 `meal_companion_db.worlds` 世界陪伴层，并恢复原稿完整的餐厅邀请池和第三项餐食抽取概率。
  - CBI办公室移除网格底纹及棕／灰／蓝灰杂色，家具统一为与我的房间相同的金色淡彩，Jane区域保留既有绿色。
  - 办公室与 Boss 家的地点按钮改用其他房间标签相同的继承字体、8px字号、500字重与1.2px字距；Boss家新增物件统一回金／绿房间色。
- 验证：实际餐食跨世界延续、陪伴层隔离、CBI空角色层、Claude原稿组件存在、CBI字体与色板约束均加入回归测试；完整测试集、HTML脚本解析与 `git diff --check` 通过。
- 请勿误改：不要重新给每日饮食套独立卡片设计；不要给 CBI 办公室恢复网格或棕灰多色板；地点按钮虽然可点击，视觉上必须继续表现为普通房间标签。

## 2026-08-24｜CBI 现实任务循环第一版

- 执行者：GPT
- 分支：`feature/cbi-life-loop` → `main`
- 提交：见本次 `main` 提交
- 涉及文件：`cbi-data.js`、`cbi-work.js`、`cbi-shop.js`、`cbi-wallet.js`、`daily.html`、`shop.html`、`wallet.html`、`schedule.html`、`character-runtime.js`、`index.html`、`world-context.js`、`cloud-sync.js`、测试与架构文档。
- 用户目标：先把已经没有大冲突的 CBI 现实任务剧情壳做成可使用版本，再在实际使用中调整概率、价格、台词和节奏。
- 实际改动：
  - CBI 日课产出独立工资；行动可立为无名案并按角色概率每日推进；完成现实目标时冻结并结算押凶手积分。
  - 委托池可自行添加“知道该做但不会主动安排”的任务，每天抽一份角色委托，奖励宝石与发起人好感；预置 Lisbon 无花费日和 Jane 清理洗手池两例。
  - CBI 工资商城接入精选、自选、衣柜与采购记录；忍者神龟卫衣等首批商品可购买并显示团队反应，服装按世界写入 Boss／Jane 衣柜。
  - 日程页新增 Boss 办公室／临时外勤／全天外勤、同行名单、全员出动、工作餐提议人、经费批复与中途归队；角色值班优先读取这份调度。
  - 原现实钱包作为 CBI 大案基金与真实记账入口；未复制第二套账本，工资、宝石与现实基金互不混用。
  - 进行中的正式案宗可按次批准调查经费，随机生成当前在场成员的一句短进展；支出会占用案件基金，累计 100 后由 Boss 手动归档，不自动编写案件事实。
- 验证：新增 CBI 数据兼容、每日推进幂等、多人结算、委托每日限制、正式案宗经费推进和全员外勤测试；完整 35 项测试、六个改动 HTML 的内联脚本解析、全部新增 JavaScript 语法与 `git diff --check` 通过。
- 遗留：运行环境没有 Chromium 可执行文件，未完成真实浏览器点击与截图；第一版数值、商品、委托台词和经费触发仍应根据实际使用继续调。
- 请勿误改：不要把 CBI 工资／宝石写进 `daily_db` 或 `bean_st`；不要恢复 CBI 日常／钱包／商城空路由；调度记录存在时必须允许全员外勤，不要强制留一人在办公室；带 `worldId: cbi` 的衣柜项不得在夹缝空间显示。

## 2026-08-25｜CBI 原版关系与年轻小组恢复

- 执行者：GPT
- 分支：`main`
- 涉及文件：`cbi-data.js`、`index.html`、`cbi-shop.js`、CBI入口缓存版本、测试与决策记录。
- 用户目标：废弃把成员写成与 Boss 无关的成熟独立角色的重制版，恢复由 Milo 组建、带教并成为所有核心关系中心的原版小组。
- 实际改动：
  - 新增一次性人物档案迁移，只重写 Milo、Jane、Cho、Rigsby、Lisbon、Van Pelt 六名核心成员；保留案件、主线、工作进度和其他自建人物。旧值进入既有云历史保护后再同步。
  - 锁定年龄与履历：Milo 27岁／6—7年经验／大学时期人质谈判；Rigsby 27岁；Lisbon 25岁／地方警局三年／无大案与重要导师；Van Pelt 23岁／一年专项培训。
  - Lisbon 改回因 Milo 留下 Jane 造成的临时空缺而提前调入、要向 Milo 证明自己的年轻探员；删除索要现场名单、核查组长流程、评估或接管 Boss 的台词，所有疑问改为向上报告或请求授权。
  - Jane 办公室台词从围绕 Lisbon 改为分别观察 Cho、Rigsby、Van Pelt、Lisbon 与全组；Lisbon 只保留一条正常评价，Jane 对 Milo 的额外注意藏在整体台词中，不逐句声明。
  - 自动调查短句恢复为成员自然陈述发现；Lisbon 的预置委托从命令 Boss 节约改为请 Milo 带她熟悉第一次报销。
- 验证：85项完整测试、JavaScript语法检查与 `git diff --check` 全部通过；新增回归约束确保旧名单台词不再出现、Jane办公室不再由Lisbon独占、人物档案迁移不触碰案件正文。
- 请勿误改：不要把成熟原剧 Lisbon 的履历重新塞回开局；不要让局部授权变成接管全局；不要让 NPC 否定、重做或夺走 Boss 已经给出的线索；Jane 可以评价任何人，但不能把任何人写成比 Milo 更核心的关系。

## 2026-08-25｜CBI 群像台词去口号化

- 用户反馈：上一轮为修复主角位置而矫枉过正，普通工作台词几乎句句强调 Boss 拍板，群像显得机械。
- 实际改动：保留人物年龄、履历、Lisbon 权限边界、Jane 感情重心与主角高光保护；办公室闲聊、自动调查进展、Jane 居家台词和商城反应改回自然表达。成员可自行跑现场、整理资料、判断和提出下一步，不必逐句请示。
- 新边界：权力约束只在全组分工、越权接管和高光归属发生冲突时生效；日常对话通过行动与关系体现 Milo 的位置，不使用反复点名来证明。
- 验证：人物档案迁移升至 canonVersion 2，确保已经加载上一版的设备也能收到降调后的档案；测试限制办公室台词中的机械 Boss 重复。

## 2026-09-02｜CBI 办公室连续在场状态

- 用户反馈：CBI 办公室立绘在零点整批变化，看起来像 24:00 换日；人物全天只有办公室／外勤两态，Jane 还会随着 Boss 家与办公室页面切换而瞬移。
- 实际改动：新增按 04:00 工作日稳定生成的到岗、下班、短时离席、半天／全天外勤时间线；角色到岗区间按 Cho／Lisbon、Van Pelt、Rigsby 的先后倾向区分，Jane 另有接近中午才到岗的日期。人工调度继续优先于自动安排。
- 首页接入：空工位现在可点击查看当前去向和“快回来了／还要好一阵／可能会很晚”等模糊归期；Boss 家的 Jane 只在人物状态确实为 `home` 时出现，在办公室、短暂离席及外勤期间显示对应离开状态。
- 连续性边界：页面切换只查看状态，不生成或改变状态；凌晨 00:00—03:59 仍属于前一个 CBI 工作日。

## 2026-09-03｜CBI 首页双城市天气

- 执行者：GPT
- 分支：`main`
- 涉及文件：`index.html`、`cbi-weather.js`、`tests/cbi-home-weather.test.js`、架构文档。
- 用户目标：在右侧时钟上方、与世界标题齐平的位置显示 `SACRAMENTO · 天气 · 温度｜OSAKA · 天气 · 温度`，让游戏世界与现实大阪同时具有天气感。
- 实际改动：Sacramento 按首页当前月、日、小时读取 Open-Meteo 的 2006 年历史天气，Osaka 以固定坐标读取当前实况；桌面端把 Sacramento 图标与温度拆到世界版头最右端，把 Osaka 名称与天气留在右侧时钟上方，两者抬高并对齐世界标题中线，不推动原有时钟和模块；手机端继续收进世界标题区。两地天气使用独立本地缓存，Osaka 半小时刷新，历史天气跨小时更新，断网或接口失败时保留最近一次成功显示。
- 数据边界：天气只用于首页氛围，不写入 `cbi_db`，不参与剧情、人物在场、云同步或备份；切换到夹缝空间后隐藏并停止轮询。
- 验证：天气模块与首页内联脚本语法检查通过，5 项新增天气测试通过；完整测试集仍为基线已有的 12 项失败，本批没有新增回归失败。

## 2026-09-03｜Techo 分栏补齐六列

- 执行者：GPT
- 分支：`main`
- 涉及文件：`techo.html`、`tests/techo-ref-flow.test.js`、架构文档。
- 用户目标：补回 Techo 项目分栏昨天未落地的六列展示，并与本轮 CBI 首页天气位置调整一起交付。
- 实际改动：Reference 与 List 的快捷分栏编辑器、完整项目编辑器均增加 `6col`；保存、旧数据标准化与详情网格统一把合法范围设为 2—6 列，避免六列存档重新打开后被截成五列。
- 验证：Techo 项目流测试覆盖六列选项、存档保留和两类详情网格渲染；同时复验天气与世界首页相关测试、HTML 内联脚本语法及 `git diff --check`。

## 2026-09-03｜Techo 并列导航与 List 周边记录化

- 执行者：GPT
- 分支：`main`
- 涉及文件：`techo.html`、`tests/techo-ref-flow.test.js`、架构文档。
- 用户目标：取消使用成本过高的 ITEM → LIST → REF 顺序入口，让三页像记账系统一样并列；LIST 的栏目、项目和品目改用 Food 的成熟结构，同时保留金额统计与从 REF 直接添加的能力。
- 实际改动：Techo 增加固定底部 ITEM／LIST／REF 三 Tab；LIST 按分类折叠展示项目、完成进度和 2—6 列 Series，品目改为可追加多条购入记录的周边逻辑，并把金额同步到 ITEM 与 Fund Pool。REF 的 List 模式改为直接选择目标 List／Series 后保存，删除单独的待分配拖拽池；删除 List 或未购入的 REF 来源品目时会同步解除稳定回链。
- 数据边界：Fund Pool 保留，只负责汇总实际购入后生成的 ITEM 金额与出售回收；它不再承担 REF 到 LIST 的中转用途。旧 List 打卡与旧索引回链在标准化时迁为稳定的品目 ID／记录 ID，不清空用户数据。
- 验证：Techo 内联脚本解析、REF 直达 LIST、六列 Series、周边购入记录、稳定回链与 Fund Pool 金额汇总均通过针对测试；完整测试集按既有基线复验。

### 同轮补修｜图鉴栏目与品目整理

- 图鉴项目编辑器增加栏目长按排序，栏目草稿改为携带自己的品目桶；在任意中间栏目增加空格时，新格会插在本栏末尾，不再令后续栏目的已有品目整体上跳，换序时本栏品目也一起移动。
- 图鉴品目整理移除会令拖动发沉的初始纵向 `touch-action`，长按整理态补齐与 Food 相同的角落删除按钮；跨栏拖动和删除都会同步修正栏目数量，已有 List 购入记录的来源仍受回链保护。
- 针对测试增加中间栏目扩容、栏目携带品目换序、整理态删除与栏目计数修复。
- 栏目快捷编辑及完整项目编辑保存后统一恢复详情页原滚动位置；REF 与 LIST 两边同时补齐，避免修改栏目名后跳回顶端。
- Techo 底部 Tab 的无效 `font` 简写曾触发浏览器默认按钮字体；改为与 CBI 记账相同的继承字体、10px 字号和字距，恢复同一套轻量底栏观感。
- LIST 品目格与 REF 对齐：系统扩栏生成的 `Title N` 仅作内部占位，不再覆盖在图片底部；只有用户填写过的品目名才显示。
- LIST 购入打卡复用影院的 `≈ Month` 开关；开启后记录只保存年月，并在详情历史中以 `≈ YYYY-MM` 明示日期为约数。
- LIST 栏目横线从“标题＋进度”的整体中线抬高 6px，改为与栏目标题字心对齐，进度数字继续留在下方。

## 2026-09-03｜CBI 商城项目折叠与品目归属

- 执行者：GPT
- 分支：`main`
- 涉及文件：`cbi-shop.js`、`shop.html`、`shop-wallet-ui.test.js`、架构文档。
- 用户目标：三级花销栏目展开后，单个项目仍可继续收起；“适合谁”不再绑定整个项目或分栏，而是由每个具体品目分别设置。
- 实际改动：项目标题行增加独立折叠箭头，展开状态按项目 ID 本地记忆；项目编辑器移除角色选项，品目编辑器加入 Boss、Jane、Cho、Rigsby、Lisbon、Van Pelt 六人单选，再次点击当前角色可取消。未购入品目继续使用普通灰边，点亮后才把边框、无图编号与完成勾染成对应角色代表色；品目详情同步显示归属。
- 数据边界：继续使用既有 `targetIds` 保存品目归属，商城备份格式与旧品目兼容；项目的旧 duty 字段不再参与页面归属或配色，新保存项目统一回到商城金色。
